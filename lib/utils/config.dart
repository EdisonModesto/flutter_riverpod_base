/// The AppConfig class contains static boolean variables for development mode, login status, and HTTP
/// logging.
class AppConfig {
  static const devMode = true;
  static const isLoggedIn = false;
  static const logHttp = true;
}
